import { createTheme } from "@mui/material/styles";

// メイリオフォントを設定したテーマ
const theme = createTheme({
  typography: {
    fontFamily: '"Meiryo", "Hiragino Kaku Gothic ProN", "Helvetica", "Arial", sans-serif',
  },
});

export default theme;
